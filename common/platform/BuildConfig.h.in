#pragma once

// Generated by CMake. Do not edit the generated copy in the build directory.

#define OG_EXECUTION_MODE_NAME "@OPENGOAL_EXECUTION_MODE@"
#define OG_GRAPHICS_BACKEND_NAME "@OPENGOAL_GRAPHICS_BACKEND@"

#define OG_EXECUTION_MODE_AOT @OG_EXECUTION_MODE_AOT@
#define OG_EXECUTION_MODE_DYNAMIC @OG_EXECUTION_MODE_DYNAMIC@
#define OG_GRAPHICS_BACKEND_METAL @OG_GRAPHICS_BACKEND_METAL@
#define OG_GRAPHICS_BACKEND_OPENGL @OG_GRAPHICS_BACKEND_OPENGL@
#define OG_TARGET_IOS @OG_TARGET_IOS@
#define OG_TARGET_MACOS @OG_TARGET_MACOS@

namespace goal_platform {

enum class ExecutionMode { Dynamic, Aot };
enum class GraphicsBackend { OpenGL, Metal };

inline constexpr ExecutionMode kExecutionMode =
#if OG_EXECUTION_MODE_AOT
    ExecutionMode::Aot;
#else
    ExecutionMode::Dynamic;
#endif

inline constexpr GraphicsBackend kGraphicsBackend =
#if OG_GRAPHICS_BACKEND_METAL
    GraphicsBackend::Metal;
#else
    GraphicsBackend::OpenGL;
#endif

inline constexpr bool kDynamicCode = OG_EXECUTION_MODE_DYNAMIC != 0;
inline constexpr bool kAotCode = OG_EXECUTION_MODE_AOT != 0;
inline constexpr bool kMetal = OG_GRAPHICS_BACKEND_METAL != 0;
inline constexpr bool kIos = OG_TARGET_IOS != 0;
inline constexpr bool kMacos = OG_TARGET_MACOS != 0;

}  // namespace goal_platform
